import React, { Component } from 'react';
import Input from './Inputs';

class Form extends Component {
  render() {
    return (
      <form>
        <Input />
      </form>
    );
  }
}

export default Form;
